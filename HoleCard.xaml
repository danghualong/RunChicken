<UserControl x:Class="RunChicken.HoleCard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:RunChicken"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Polygon x:Name="pg" Stroke="Black" StrokeThickness="2" Fill="#eee"/>
        <Image x:Name="img" Source=".\imgs\chicken_bg.jpg" HorizontalAlignment="Center" VerticalAlignment="Center" Stretch="UniformToFill" Opacity="0.5" MouseLeftButtonDown="img_MouseLeftButtonDown"/>
        <TextBlock x:Name="tb1" Text="A" FontSize="60" Foreground="Red" 
                               HorizontalAlignment="Center" 
                               VerticalAlignment="Center"/>
    </Grid>
</UserControl>
